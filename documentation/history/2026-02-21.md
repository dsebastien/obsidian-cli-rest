# 2026-02-21

## Dynamic CLI Command Discovery

Implemented hybrid discovery + pass-through for CLI commands.

### New files

- `src/app/services/cli-command-discovery.ts` — `parseHelpOutput()` and `discoverCliCommands()` functions. Parses `obsidian help` output to discover commands. `DiscoveredCommand` type with `command`, `description`, `section` fields.
- `src/app/services/cli-command-discovery.spec.ts` — Tests with captured help output snapshot (CLI 1.12.2).

### Modified files

- `src/app/domain/cli-command-registry.ts` — Added runtime merge capability: `mergeDiscoveredCommands()`, `resetDiscoveredCommands()`, `isDangerousPattern()`. `getCommandDefinition()` and `getAllCommands()` now return merged set (static + discovered). Static entries always win.
- `src/app/plugin.ts` — Added `discoverCommands()` private method called after CLI availability check in `onload()` and `recheckCli()`. Added registration of `copy-rest-url` and `copy-mcp-url` commands.
- `src/app/services/request-router.ts` — Changed unknown command behavior from 404 to pass-through via POST. Non-POST methods for unknown commands return 405. Ephemeral definitions use safe defaults with dangerous pattern detection.
- `src/app/commands/toggle-server.ts` — Added `registerCopyRestUrlCommand()` and `registerCopyMcpUrlCommand()` functions.

### Test changes

- `src/app/domain/cli-command-registry.spec.ts` — Added `afterEach(resetDiscoveredCommands)` for test isolation. Added tests for `mergeDiscoveredCommands`, `resetDiscoveredCommands`, `isDangerousPattern`.
- `src/app/services/request-router.spec.ts` — Changed "returns 404 for unknown CLI command" test to verify 405 (GET not allowed for pass-through). Added pass-through tests: POST succeeds, DELETE returns 405, dangerous pattern detection, blocked list, CLI unavailable.

### Documentation

- Updated Architecture.md, Domain Model.md, Business Rules.md with discovery flow, `DiscoveredCommand` type, and new business rules.

### Key decisions

- POST is the only allowed method for pass-through commands (safest default).
- Static registry entries always take precedence over discovered ones.
- Discovery failure is non-fatal — graceful fallback to static-only.
- Dangerous pattern detection uses regex matching business rules (`dev:*`, `eval`, `restart`, `reload`, `devtools`, `command`, `plugins:restrict`).

## Internal Commands

Added two internal commands (`cli-rest:rest-url`, `cli-rest:mcp-url`) handled by the plugin without calling the CLI binary. These return the REST API base URL and MCP server URL respectively. They work even when CLI is unavailable. Also added command palette commands to copy these URLs to clipboard.

## OpenAPI Documentation

Added dynamic OpenAPI 3.1.0 documentation served by the REST API.

### New files

- `src/app/services/openapi-generator.ts` — `generateOpenApiSpec()` builds OpenAPI 3.1.0 spec dynamically from command registry (static + discovered). `generateDocsHtml()` produces Scalar API Reference HTML page. Spec includes all paths, schemas for request/response types, Bearer auth security scheme, and tags by category.

### Modified files

- `src/app/services/http-server.ts` — Added `/api/v1/docs` and `/api/v1/openapi.json` to public endpoints (no auth required).
- `src/app/services/request-router.ts` — Added routes for `/api/v1/openapi.json` (returns JSON spec) and `/api/v1/docs` (returns Scalar HTML). Imported `sendJson` and `sendHtml` from response-builder. Added `cli-rest:docs-url` internal command handler.
- `src/app/services/response-builder.ts` — Made `sendJson` public. Added `sendHtml()` function for HTML responses.
- `src/app/domain/cli-command-registry.ts` — Added `cli-rest:docs-url` internal command. Updated `INTERNAL_COMMANDS` set.
- `src/app/commands/toggle-server.ts` — Added `registerCopyDocsUrlCommand()`.
- `src/app/plugin.ts` — Registered `copy-docs-url` command.

### Documentation

- Updated Architecture.md: request flow, component descriptions, directory structure.
- Updated Business Rules.md: public endpoints, command palette commands count, internal commands list, new OpenAPI section.
- Updated AGENTS.md: added note about keeping OpenAPI spec up to date in the "Do" section.

## MCP Code Mode Pattern

Replaced per-command MCP tool registration (113+ tools) with 2 generic tools following the Code Mode pattern.

### Modified files

- `src/app/domain/cli-command-registry.ts` — Added `searchCommands()` function for progressive discovery with query and category filters.
- `src/app/services/mcp-server.ts` — Replaced `registerTools()`: instead of one tool per CLI command, now registers exactly 2 tools (`search` and `execute`). Fixed missing `cli-rest:docs-url` case in `handleInternalCommand()`. Updated class JSDoc.
- `src/app/domain/cli-command-registry.spec.ts` — Added tests for `searchCommands()`: no filters, category filter, query filter, case-insensitivity, combined filters, categories always returned, discovered commands included.

### Documentation

- Updated Architecture.md: MCP section describes Code Mode pattern.
- Updated Business Rules.md: added Code Mode rule.

### Key decisions

- Structured search (query + category filters) instead of JS eval — achieves progressive discovery without security risks.
- Agent discovers command parameters via `help` command at runtime — no need to maintain per-command schemas.
- `execute` tool accepts any command string (pass-through for unknown/discovered commands).
- Categories always returned in search results to guide next query.
